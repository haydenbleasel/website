import { sortByName } from '@/lib/utils';

const freelanceClients = [
  'ESLint',
  'OpenJS Foundation',
  'UTS',
  'Tyro Payments',
  'Teleqo Technologies',
].sort(sortByName);

export default freelanceClients;
