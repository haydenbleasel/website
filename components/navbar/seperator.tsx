import { Root } from '@radix-ui/react-separator';
import type { FC } from 'react';

const Separator: FC = () => (
  <Root className="h-full w-px bg-zinc-200 dark:bg-zinc-700" />
);

export default Separator;
